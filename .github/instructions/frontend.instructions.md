---
applyTo: "frontend/**"
---
- Keep state in App.jsx until a plan approves a state library.
- All API calls go through relative /api paths (the Vite proxy handles dev).
- No CSS frameworks; inline styles or a single stylesheet.
