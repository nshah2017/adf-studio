# ADR 0002: <title>
# Lab 06 (D3): the "created-at timestamp" feature needs a decision recorded
# so future agents (and humans) inherit it. ADR 0001 constrains you: storage
# stays a JSON file. Write the ADR so an agent reading it six months from
# now makes the same choices you did.

Status: proposed
Date: ____FILL_ME____

## Context
<!-- TODO(D3): why does this decision need recording at all? Name what ADR
     0001 already fixes and what this ADR adds on top. -->
____FILL_ME____

## Decision
<!-- TODO(D3): pick and commit: field name, format (hint: ISO 8601 UTC),
     where it is set (storage layer or endpoint?), and what happens to the
     tasks already in data/tasks.json that lack the field. -->
____FILL_ME____

## Consequences
<!-- TODO(D3): at least one benefit, one cost, and one instruction to future
     agents (what must they read or preserve?). -->
____FILL_ME____
